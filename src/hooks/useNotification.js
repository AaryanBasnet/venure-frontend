// Re-export bridge — NotificationDropdown imports { useNotifications } from
// this path and continues to work without any changes.
export { useNotifications } from "../features/notifications/hooks/useNotifications";
